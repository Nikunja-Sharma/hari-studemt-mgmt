# Vite automatically exposes import.meta.env.DEV in development mode
# No additional configuration needed for dev mode detection

# API Base URL (optional - defaults to http://localhost:3000)
# VITE_API_URL=http://localhost:3000

# Note: In production builds, import.meta.env.DEV will be false
# and test login buttons will be automatically hidden
